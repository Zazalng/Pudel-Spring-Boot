# ===========================================
# Docker Ignore File
# Excludes unnecessary files from Docker build
# ===========================================

# Git
.git
.gitignore
.github

# IDE
.idea
*.iml
.vscode
.project
.classpath
.settings

# Build artifacts
**/target
!pudel-core/target/*.jar

# Documentation
docs
*.md
!README.md

# Vue frontend (built separately)
vue

# Environment files (use docker-compose env)
.env
.env.local
.env.*.local

# Logs
logs
*.log

# OS files
.DS_Store
Thumbs.db

# Test files
**/src/test

# Note: scripts/docker-entrypoint.sh is needed for Docker image
# Other scripts (start.sh, update.sh) are for host use only

# Docker files (prevent recursive copy)
Dockerfile*
docker-compose*
.dockerignore

# Keys (mounted at runtime, not baked into image)
keys